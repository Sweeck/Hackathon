## Customize the test machine

general:
  build_dir: PHP

test:
  override:
    - php ./index.php --unit-tests


database:
  override:
    - mysql -u ubuntu circle_test <  db.opt

deployment:
  prod:
    branch: master
    heroku:
      appname: Hackathon